Malline
=======

Example template file images.mn:

html do
	render :partial => 'head'
	body do
		div.images! "There are some images:" do
			images.each do |im|
				a :href => img_path(im) { img :src => im.url }
				span.caption im.caption
			end
			text! "No more images"
		end
		div.footer! { render :partial => 'footer' }
	end
end
