xhtml do
	head do
		title "MallineTests: #{controller.action_name}"
		_stylesheet_link_tag 'scaffold'
	end
	body do
		p flash[:notice], :style => 'color: green'
		self << @content_for_layout
	end
end
